name: Tests
on:
  push:
    branches:
    - dev
  workflow_dispatch:


env:
  api_id: 1170144
  api_hash: '7cf4f6769b5cfcd1c3b7bd645779c634'
  session: ${{ secrets.session }}
  redis_uri: 'redis-19123.c282.east-us-mz.azure.cloud.redislabs.com:19123'
  redis_password: ${{ secrets.redis_password }}

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run
        uses: ./
